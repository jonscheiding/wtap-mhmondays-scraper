{
  "editor.formatOnSave": true,
  "emeraldwalk.runonsave": {
    "commands": [
      {
        "match": "package\\.json",
        "cmd": "pnpm sort-package-json ${file} && echo ${file} formatted"
      }
    ]
  }
}
